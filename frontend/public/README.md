# Public assets

Files here are copied unchanged to the Vite build. `hero-auction-marketplace.png` supports the existing homepage. Reference assets with root-relative URLs and place only immutable public files here; imported component assets should live near their feature. Production media should be supplied in responsive, optimized variants.
