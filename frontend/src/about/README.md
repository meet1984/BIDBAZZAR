# About page

`AboutPage.jsx` renders the company and marketplace explanation; `index.jsx` is its public export. It uses shared navigation, footer, link, and image components and does not own server data. Approved brand-copy changes belong here.
