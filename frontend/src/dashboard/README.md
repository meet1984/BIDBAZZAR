# Dashboard foundations

`DashboardLayout.jsx` provides the reusable responsive shell. Buyer, seller and full-admin pages call role-specific endpoints; admin employees use the permission-aware portal. Every page renders explicit empty, error and loading states and uses server-authoritative metrics.
