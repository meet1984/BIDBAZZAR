# Home page

`HomePage.jsx` loads approved featured listings, sends discovery searches to `/auctions`, and uses authenticated buyer watchlist actions; `index.jsx` exports it. Homepage-only presentation belongs here and reusable listing UI belongs in `components`.
