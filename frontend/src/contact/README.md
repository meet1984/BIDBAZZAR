# Contact and support

`ContactPage.jsx` submits validated `FormData`, including an optional attachment, to `/api/support/enquiries`; `index.jsx` exports the page. It displays only backend-confirmed references and never simulates success. Policy-approved support fields and consent copy belong here.
