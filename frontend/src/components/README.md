# Shared components

This folder contains BidMyLot-wide presentation primitives: navigation, footer, brand, links, images, and async-state components. Feature pages compose these pieces; components may call shared contexts or API helpers only when behavior is truly global. Export public components from `index.jsx`.
