# Frontend libraries

`api.js` configures Axios, in-memory access tokens, credentials and serialized session refresh. `format.js` centralizes display formatting and safe API error messages. Pages consume these helpers rather than creating clients.
