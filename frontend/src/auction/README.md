# Auction discovery

`AuctionListingPage.jsx` renders URL-synchronized filters, pagination, API-backed public results, and buyer watchlist actions; `index.jsx` exports the page. Query state is sent to `GET /api/auctions`, and returned pagination/status values remain authoritative.
