# Dashboard module

This read-only composition module returns data owned by listings, watchlists, legacy bids, accounts and support. Routes are role-specific; the service shapes buyer, seller and full-admin responses. Admin employees use permission-specific endpoints instead of the full-admin aggregate.
