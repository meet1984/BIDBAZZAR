# Bidding module

Public history and buyer-only legacy bid placement live here. The service validates live state, ownership and the server-derived minimum; the repository locks the auction row and persists the result in one transaction. New marketplace sales should use negotiated or multi-unit offers.
