# Auctions module

This legacy-compatible module owns public discovery/detail plus seller and admin auction management. Schemas define filters and editable fields, services enforce ownership/lifecycle/schedule rules, and the repository maps public status and executes auction SQL. New negotiated and multi-unit commerce belongs to the listings and offers modules.
